<h2><%= link_to(post.title(current_lang), post) %></h2>

<ul class="unstyled inline">
  <li><%= time_ago(post.created_at) %></li>
  <li><%= link_to(post.category.name(current_lang), post.category) %></li>
</ul>

<div>
<%#= truncate_html(post.html(current_lang), length: @settings.post.truncate, omission: "... #{link_to(t('continue'), post)}").html_safe %>
<%= truncate_html(post.html(current_lang), length: @settings.post.truncate).html_safe %>
</div>


